import { ControlConfig } from '../types';
export declare const defaultConfig: {
    type: "number";
    value: number;
};
export declare const defaultValue: (config: ControlConfig) => any;
export declare const clamp: (num: number, clamp: number, higher: number) => number;
export declare const map: (value: number, x1: number, y1: number, x2: number, y2: number) => number;
